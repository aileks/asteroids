[project]
name = "asteroids"
version = "0.1.0"
description = "A modern, feature-rich implementation of the classic Asteroids arcade game"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pygame==2.6.1",
]

[dependency-groups]
dev = [
    "ruff>=0.14.8",
]
